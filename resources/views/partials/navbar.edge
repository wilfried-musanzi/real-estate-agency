<header class="header">
    <a href="{{ route('home') }}" class="header__brand"><span>jmk</span></a>
    <nav class="header__nav">
      <ul class="nav__list">
        <li class="nav__item">
          @if(auth.user) 
            <a href="{{ route('me') }}" hx-target="body" class="user__account">{{ auth.user.username }}</a> 
          @else
            <a href="{{ route('login') }}" class="nav__link">Connexion
          @end
          </a>
        </li>
        <li class="nav__item">
        @if(auth.user)
          <a href="{{ route('logout') }}"  class="user__logout"><i class="ph-sign-out"></i></a> 
        @else
          <a href="{{ route('signup') }}" class="nav__link link__cta">Inscrption</a>
        @end
        </li>
      </ul>
    </nav>
</header>
